import type { MysqlProblem } from '../types'

function problem(problem: MysqlProblem): MysqlProblem {
  return problem
}

export const mysqlProblems: MysqlProblem[] = [
  problem({
    id: 'select-basic',
    title: '查询所有用户',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 5,
    description: '查询 users 表中的全部字段，并按 id 升序返回结果。',
    requirements: ['使用 SELECT 查询全部字段', '结果按 id 升序排序'],
    knowledgePoints: ['SELECT', 'FROM', 'ORDER BY'],
    relatedTables: ['users'],
    starterSql: '-- 查询 users 表中的全部数据\nSELECT \n',
    solutionSql: 'SELECT * FROM users ORDER BY id;',
    explanation: '最基础的查询题，重点是熟悉数据集和返回顺序。',
    interviewTips: ['SELECT * 虽然方便，但在线上业务中通常建议显式列名。'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'select-columns',
    title: '查询指定字段',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 5,
    description: '查询 users 表中的 id、name、city 三个字段，并按 id 升序排序。',
    requirements: ['只返回指定列', '按 id 升序返回'],
    knowledgePoints: ['列选择', 'SELECT', 'ORDER BY'],
    relatedTables: ['users'],
    starterSql: '-- 只查询 id, name, city\nSELECT \n',
    solutionSql: 'SELECT id, name, city FROM users ORDER BY id;',
    explanation: '手动选择列可以减少 IO，也有利于索引覆盖。',
    interviewTips: ['追问：什么时候不要写 SELECT *？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'where-filter',
    title: '筛选北京用户',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 5,
    description: '查询 city 为 Beijing 的用户姓名和邮箱，并按 id 升序返回。',
    requirements: ['使用 WHERE 做条件过滤', '返回 name、email 两列'],
    knowledgePoints: ['WHERE', '条件过滤'],
    relatedTables: ['users'],
    starterSql: "-- 查询 Beijing 用户\nSELECT name, email\nFROM users\n",
    solutionSql: "SELECT name, email FROM users WHERE city = 'Beijing' ORDER BY id;",
    explanation: 'WHERE 在 GROUP BY 之前执行，用于原始行过滤。',
    interviewTips: ['追问：WHERE 和 HAVING 的执行阶段有什么区别？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'and-or-filter',
    title: 'AND / OR 条件组合',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 4,
    description: '查询 category 为 Electronics 且 stock 大于等于 50，或者 category 为 Office 的商品。',
    requirements: ['正确使用 AND / OR 组合条件', '返回 id、name、category、stock'],
    knowledgePoints: ['AND', 'OR', '布尔优先级'],
    relatedTables: ['products'],
    starterSql: '-- 查询指定商品\nSELECT id, name, category, stock\nFROM products\n',
    solutionSql:
      "SELECT id, name, category, stock FROM products WHERE (category = 'Electronics' AND stock >= 50) OR category = 'Office' ORDER BY id;",
    explanation: 'AND 的优先级高于 OR，复杂条件建议显式加括号。',
    interviewTips: ['追问：不用括号会不会影响结果？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 6,
  }),
  problem({
    id: 'in-filter',
    title: 'IN 条件查询',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 4,
    description: '查询 city 在 Beijing 和 Shanghai 的用户。',
    requirements: ['使用 IN 简化多个等值条件', '结果按 id 升序'],
    knowledgePoints: ['IN', '条件集合'],
    relatedTables: ['users'],
    starterSql: '-- 查询指定城市的用户\nSELECT id, name, city\nFROM users\n',
    solutionSql:
      "SELECT id, name, city FROM users WHERE city IN ('Beijing', 'Shanghai') ORDER BY id;",
    explanation: 'IN 是多个 OR 的可读性更好的写法。',
    interviewTips: ['追问：IN 和 EXISTS 的典型适用场景有什么不同？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'between-filter',
    title: 'BETWEEN 范围查询',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 4,
    description: '查询 salary 在 9000 到 15000 之间的员工。',
    requirements: ['使用 BETWEEN', '返回员工 id、name、salary'],
    knowledgePoints: ['BETWEEN', '范围条件'],
    relatedTables: ['employees'],
    starterSql: '-- 查询薪资范围内的员工\nSELECT id, name, salary\nFROM employees\n',
    solutionSql:
      'SELECT id, name, salary FROM employees WHERE salary BETWEEN 9000 AND 15000 ORDER BY salary DESC, id;',
    explanation: 'BETWEEN 是包含边界值的。',
    interviewTips: ['追问：BETWEEN 是否包含上下边界？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'like-search',
    title: 'LIKE 模糊匹配',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 4,
    description: '查询产品名称中包含 Keyboard 的商品。',
    requirements: ['使用 LIKE', '返回 id、name'],
    knowledgePoints: ['LIKE', '模糊搜索'],
    relatedTables: ['products'],
    starterSql: '-- 查询名称包含关键字的商品\nSELECT id, name\nFROM products\n',
    solutionSql: "SELECT id, name FROM products WHERE name LIKE '%Keyboard%' ORDER BY id;",
    explanation: '前后都带 % 会导致索引更难命中。',
    interviewTips: ['追问：为什么前缀模糊和全模糊对索引影响不同？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'is-null',
    title: '查询没有直属上级的员工',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 4,
    description: '查询 manager_id 为 NULL 的员工，通常表示部门负责人或最高管理者。',
    requirements: ['使用 IS NULL', '返回 id、name、department_id'],
    knowledgePoints: ['NULL', 'IS NULL'],
    relatedTables: ['employees'],
    starterSql: '-- 查询没有直属上级的员工\nSELECT id, name, department_id\nFROM employees\n',
    solutionSql:
      'SELECT id, name, department_id FROM employees WHERE manager_id IS NULL ORDER BY id;',
    explanation: 'NULL 不能用 = 比较，必须用 IS NULL / IS NOT NULL。',
    interviewTips: ['追问：为什么 `manager_id = NULL` 不会得到正确结果？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'order-by',
    title: '按工资排序',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 5,
    description: '按 salary 从高到低排序员工，如果工资相同再按 id 升序。',
    requirements: ['使用多个排序字段', '返回 id、name、salary'],
    knowledgePoints: ['ORDER BY', '多字段排序'],
    relatedTables: ['employees'],
    starterSql: '-- 按工资排序\nSELECT id, name, salary\nFROM employees\n',
    solutionSql:
      'SELECT id, name, salary FROM employees ORDER BY salary DESC, id ASC;',
    explanation: '面试里经常会继续追问 order by 是否会触发 filesort。',
    interviewTips: ['追问：ORDER BY 能否利用索引？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'limit-basic',
    title: '查询最近三笔订单',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 4,
    description: '查询 created_at 最新的三笔订单。',
    requirements: ['使用 ORDER BY + LIMIT', '返回 id、user_id、status、created_at'],
    knowledgePoints: ['LIMIT', '排序分页'],
    relatedTables: ['orders'],
    starterSql: '-- 查询最近三笔订单\nSELECT id, user_id, status, created_at\nFROM orders\n',
    solutionSql:
      'SELECT id, user_id, status, created_at FROM orders ORDER BY created_at DESC, id DESC LIMIT 3;',
    explanation: 'LIMIT 通常配合稳定排序字段一起使用。',
    interviewTips: ['追问：深分页为什么慢？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'distinct-city',
    title: '去重查询城市',
    chapter: '01 基础查询',
    level: 1,
    difficulty: 'Easy',
    frequency: 4,
    description: '查询 users 表中所有不同的城市名称。',
    requirements: ['使用 DISTINCT', '结果按 city 升序'],
    knowledgePoints: ['DISTINCT', '去重'],
    relatedTables: ['users'],
    starterSql: '-- 查询不同城市\nSELECT \n',
    solutionSql: 'SELECT DISTINCT city FROM users ORDER BY city;',
    explanation: 'DISTINCT 常常可以和索引一起讨论。',
    interviewTips: ['追问：DISTINCT 和 GROUP BY 在简单去重场景里的区别是什么？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'count-orders',
    title: '统计已支付订单数',
    chapter: '02 聚合函数',
    level: 2,
    difficulty: 'Easy',
    frequency: 5,
    description: '统计状态为 paid 的订单数量。',
    requirements: ['使用 COUNT', '结果列命名为 paid_order_count'],
    knowledgePoints: ['COUNT', '聚合函数'],
    relatedTables: ['orders'],
    starterSql: '-- 统计已支付订单数\nSELECT \n',
    solutionSql:
      "SELECT COUNT(*) AS paid_order_count FROM orders WHERE status = 'paid';",
    explanation: 'COUNT(*) 不会忽略行，只统计满足条件的记录数。',
    interviewTips: ['追问：COUNT(*)、COUNT(1)、COUNT(column) 的区别是什么？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 6,
  }),
  problem({
    id: 'sum-order-amount',
    title: '统计已支付订单总金额',
    chapter: '02 聚合函数',
    level: 2,
    difficulty: 'Easy',
    frequency: 5,
    description: '统计所有已支付订单的 total_amount 之和。',
    requirements: ['使用 SUM', '结果列命名为 total_paid_amount'],
    knowledgePoints: ['SUM', '聚合统计'],
    relatedTables: ['orders'],
    starterSql: '-- 统计已支付订单总金额\nSELECT \n',
    solutionSql:
      "SELECT SUM(total_amount) AS total_paid_amount FROM orders WHERE status = 'paid';",
    explanation: '聚合统计题常常是后续 GROUP BY 的前置基础。',
    interviewTips: ['追问：SUM 遇到 NULL 会如何处理？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 6,
  }),
  problem({
    id: 'avg-salary',
    title: '计算工程部平均薪资',
    chapter: '02 聚合函数',
    level: 2,
    difficulty: 'Easy',
    frequency: 4,
    description: '计算 Engineering 部门员工的平均薪资。',
    requirements: ['使用 AVG', '只统计 Engineering 部门'],
    knowledgePoints: ['AVG', '聚合函数', 'WHERE'],
    relatedTables: ['employees', 'departments'],
    starterSql: '-- 查询工程部平均薪资\nSELECT \n',
    solutionSql:
      "SELECT ROUND(AVG(e.salary), 2) AS avg_salary FROM employees e INNER JOIN departments d ON e.department_id = d.id WHERE d.name = 'Engineering';",
    explanation: '这题把聚合和 JOIN 组合在一起，属于真实业务里很常见的模式。',
    interviewTips: ['追问：AVG 是否会自动跳过 NULL？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 8,
  }),
  problem({
    id: 'max-min-salary',
    title: '查询最高和最低工资',
    chapter: '02 聚合函数',
    level: 2,
    difficulty: 'Easy',
    frequency: 4,
    description: '查询 employees 表中的最高工资和最低工资。',
    requirements: ['使用 MAX 和 MIN', '列名分别为 max_salary、min_salary'],
    knowledgePoints: ['MAX', 'MIN'],
    relatedTables: ['employees'],
    starterSql: '-- 查询最高和最低工资\nSELECT \n',
    solutionSql:
      'SELECT MAX(salary) AS max_salary, MIN(salary) AS min_salary FROM employees;',
    explanation: '这类题经常是“第二高工资”之类题目的基础变形。',
    interviewTips: ['追问：如何进一步查出拿到最高工资的员工？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 5,
  }),
  problem({
    id: 'group-by-city',
    title: '按城市统计用户数量',
    chapter: '02 聚合函数',
    level: 2,
    difficulty: 'Easy',
    frequency: 5,
    description: '按 city 分组统计用户数量。',
    requirements: ['使用 GROUP BY', '结果列命名为 user_count'],
    knowledgePoints: ['GROUP BY', 'COUNT'],
    relatedTables: ['users'],
    starterSql: '-- 统计每个城市的用户数\nSELECT city, \nFROM users\n',
    solutionSql:
      'SELECT city, COUNT(*) AS user_count FROM users GROUP BY city ORDER BY user_count DESC, city;',
    explanation: 'GROUP BY 是后续分组聚合和 Top N 题的基础。',
    interviewTips: ['追问：SELECT 中为什么不能随便出现未分组字段？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 6,
  }),
  problem({
    id: 'having-filter',
    title: '筛选用户数不少于 2 的城市',
    chapter: '02 聚合函数',
    level: 2,
    difficulty: 'Medium',
    frequency: 5,
    description: '按城市统计用户数，并只保留人数不少于 2 的城市。',
    requirements: ['使用 GROUP BY', '使用 HAVING 过滤聚合结果'],
    knowledgePoints: ['HAVING', 'GROUP BY', 'COUNT'],
    relatedTables: ['users'],
    starterSql: '-- 筛选用户数不少于 2 的城市\nSELECT city, COUNT(*) AS user_count\nFROM users\n',
    solutionSql:
      'SELECT city, COUNT(*) AS user_count FROM users GROUP BY city HAVING COUNT(*) >= 2 ORDER BY user_count DESC, city;',
    explanation: 'WHERE 过滤原始行，HAVING 过滤聚合后的分组结果。',
    interviewTips: ['追问：HAVING 能不能被 WHERE 替换？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 8,
  }),
  problem({
    id: 'case-when-orders',
    title: 'CASE WHEN 订单分层',
    chapter: '02 聚合函数',
    level: 2,
    difficulty: 'Medium',
    frequency: 4,
    description:
      '给 orders 表中的订单打标签：total_amount >= 5000 为 VIP，>= 1000 为 High，其他为 Normal。',
    requirements: ['使用 CASE WHEN', '返回 id、total_amount、order_level'],
    knowledgePoints: ['CASE WHEN', '条件分支'],
    relatedTables: ['orders'],
    starterSql: '-- 给订单打等级标签\nSELECT id, total_amount, \nFROM orders\n',
    solutionSql:
      "SELECT id, total_amount, CASE WHEN total_amount >= 5000 THEN 'VIP' WHEN total_amount >= 1000 THEN 'High' ELSE 'Normal' END AS order_level FROM orders ORDER BY id;",
    explanation: 'CASE WHEN 是报表和分析题里的高频语法。',
    interviewTips: ['追问：CASE WHEN 能否写在 ORDER BY 或聚合里？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 8,
  }),
  problem({
    id: 'string-function',
    title: '字符串函数练习',
    chapter: '02 聚合函数',
    level: 2,
    difficulty: 'Easy',
    frequency: 4,
    description: '把用户姓名和邮箱拼接成联系信息，格式为 `name <email>`。',
    requirements: ['使用 CONCAT', '返回 id、contact_info'],
    knowledgePoints: ['CONCAT', '字符串函数'],
    relatedTables: ['users'],
    starterSql: '-- 拼接联系信息\nSELECT id, \nFROM users\n',
    solutionSql:
      "SELECT id, CONCAT(name, ' <', email, '>') AS contact_info FROM users ORDER BY id;",
    explanation: '字符串函数在数据清洗和报表展示中很常见。',
    interviewTips: ['追问：MySQL 常见字符串函数还有哪些？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 6,
  }),
  problem({
    id: 'date-function',
    title: '按月份统计订单数',
    chapter: '02 聚合函数',
    level: 2,
    difficulty: 'Medium',
    frequency: 4,
    description: '按月份统计订单数量，结果列命名为 order_month 和 order_count。',
    requirements: ['使用日期函数', '按月份分组统计'],
    knowledgePoints: ['DATE_FORMAT', '日期函数', 'GROUP BY'],
    relatedTables: ['orders'],
    starterSql: '-- 按月份统计订单数\nSELECT \nFROM orders\n',
    solutionSql:
      "SELECT DATE_FORMAT(created_at, '%Y-%m') AS order_month, COUNT(*) AS order_count FROM orders GROUP BY DATE_FORMAT(created_at, '%Y-%m') ORDER BY order_month;",
    explanation: '日期归档是业务统计题非常高频的场景。',
    interviewTips: ['追问：按日期函数分组时对索引会有什么影响？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 8,
  }),
  problem({
    id: 'inner-join',
    title: '查询订单对应的用户',
    chapter: '03 多表查询',
    level: 3,
    difficulty: 'Easy',
    frequency: 5,
    description: '查询已支付订单及其对应的用户名称。',
    requirements: ['使用 INNER JOIN', '返回 order_id、user_name、total_amount'],
    knowledgePoints: ['INNER JOIN', '关联查询'],
    relatedTables: ['users', 'orders'],
    starterSql: '-- 查询已支付订单和用户名称\nSELECT \n',
    solutionSql:
      "SELECT o.id AS order_id, u.name AS user_name, o.total_amount FROM orders o INNER JOIN users u ON o.user_id = u.id WHERE o.status = 'paid' ORDER BY o.id;",
    explanation: 'INNER JOIN 只返回两侧都能匹配上的记录。',
    interviewTips: ['追问：INNER JOIN 和 LEFT JOIN 的结果差异是什么？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 8,
  }),
  problem({
    id: 'left-join',
    title: '统计每个用户的订单数量',
    chapter: '03 多表查询',
    level: 3,
    difficulty: 'Medium',
    frequency: 5,
    description: '查询每个用户的订单数量，包括没有下单的用户。',
    requirements: ['使用 LEFT JOIN', '返回 user_id、user_name、order_count'],
    knowledgePoints: ['LEFT JOIN', 'COUNT', 'GROUP BY'],
    relatedTables: ['users', 'orders'],
    starterSql: '-- 统计每个用户的订单数量\nSELECT \n',
    solutionSql:
      'SELECT u.id AS user_id, u.name AS user_name, COUNT(o.id) AS order_count FROM users u LEFT JOIN orders o ON u.id = o.user_id GROUP BY u.id, u.name ORDER BY u.id;',
    explanation: 'LEFT JOIN 可以保留主表里的完整用户集合。',
    interviewTips: ['追问：COUNT(*) 和 COUNT(o.id) 在 LEFT JOIN 下有什么不同？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 10,
  }),
  problem({
    id: 'right-join',
    title: '查询所有部门及员工',
    chapter: '03 多表查询',
    level: 3,
    difficulty: 'Medium',
    frequency: 3,
    description: '查询所有部门及其员工名称，部门没有员工时也要展示。',
    requirements: ['使用 RIGHT JOIN', '返回 department_name、employee_name'],
    knowledgePoints: ['RIGHT JOIN', '外连接'],
    relatedTables: ['departments', 'employees'],
    starterSql: '-- 查询所有部门及员工\nSELECT \n',
    solutionSql:
      'SELECT d.name AS department_name, e.name AS employee_name FROM employees e RIGHT JOIN departments d ON e.department_id = d.id ORDER BY d.id, e.id;',
    explanation: 'RIGHT JOIN 语义等价于交换表顺序后的 LEFT JOIN。',
    interviewTips: ['追问：为什么很多团队更偏好统一只写 LEFT JOIN？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 10,
  }),
  problem({
    id: 'multi-join',
    title: '查询订单商品明细',
    chapter: '03 多表查询',
    level: 3,
    difficulty: 'Medium',
    frequency: 5,
    description: '查询每笔订单对应的用户和商品明细。',
    requirements: ['至少关联 users、orders、order_items、products 四张表', '返回 user_name、order_id、product_name、quantity、item_amount'],
    knowledgePoints: ['多表 JOIN', '订单模型'],
    relatedTables: ['users', 'orders', 'order_items', 'products'],
    starterSql: '-- 查询订单商品明细\nSELECT \n',
    solutionSql:
      'SELECT u.name AS user_name, o.id AS order_id, p.name AS product_name, oi.quantity, oi.quantity * oi.price AS item_amount FROM orders o INNER JOIN users u ON o.user_id = u.id INNER JOIN order_items oi ON o.id = oi.order_id INNER JOIN products p ON oi.product_id = p.id ORDER BY o.id, oi.id;',
    explanation: '这是业务题里非常典型的订单主表 + 明细表 JOIN。',
    interviewTips: ['追问：为什么订单金额通常会冗余保存在 orders 表里？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 12,
  }),
  problem({
    id: 'self-join',
    title: '自连接查询员工与直属上级',
    chapter: '03 多表查询',
    level: 3,
    difficulty: 'Medium',
    frequency: 5,
    description: '查询每位员工及其直属上级姓名，没有直属上级的员工也需要显示。',
    requirements: ['使用自连接', '返回 employee_name、manager_name'],
    knowledgePoints: ['自连接', '层级关系'],
    relatedTables: ['employees'],
    starterSql: '-- 自连接查询员工和直属上级\nSELECT \n',
    solutionSql:
      'SELECT e.name AS employee_name, m.name AS manager_name FROM employees e LEFT JOIN employees m ON e.manager_id = m.id ORDER BY e.id;',
    explanation: '组织架构、区域树、分类树等题都可能用到自连接。',
    interviewTips: ['追问：如果要查多级上级关系，还有哪些做法？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 10,
  }),
  problem({
    id: 'union-basic',
    title: 'UNION 去重合并结果',
    chapter: '03 多表查询',
    level: 3,
    difficulty: 'Medium',
    frequency: 4,
    description: '分别查询 Beijing / Shanghai 城市和 Shanghai / Chengdu 城市，再使用 UNION 合并成去重结果。',
    requirements: ['使用 UNION', '返回单列 city'],
    knowledgePoints: ['UNION', '去重合并'],
    relatedTables: ['users'],
    starterSql: '-- 使用 UNION 合并结果\nSELECT city FROM users WHERE city IN (\'Beijing\', \'Shanghai\')\n',
    solutionSql:
      "SELECT city FROM users WHERE city IN ('Beijing', 'Shanghai') UNION SELECT city FROM users WHERE city IN ('Shanghai', 'Chengdu') ORDER BY city;",
    explanation: 'UNION 会自动去重，UNION ALL 不去重。',
    interviewTips: ['追问：UNION 和 UNION ALL 的性能差异在哪里？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 8,
  }),
  problem({
    id: 'join-group-by',
    title: '统计每个用户的已支付总金额',
    chapter: '03 多表查询',
    level: 3,
    difficulty: 'Medium',
    frequency: 5,
    description: '统计每个用户已支付订单的总金额，没有已支付订单的用户显示 0。',
    requirements: ['JOIN + GROUP BY', '用 COALESCE 处理空值', '返回 user_id、user_name、paid_amount'],
    knowledgePoints: ['JOIN', 'GROUP BY', 'COALESCE'],
    relatedTables: ['users', 'orders'],
    starterSql: '-- 统计每个用户的已支付总金额\nSELECT \n',
    solutionSql:
      "SELECT u.id AS user_id, u.name AS user_name, COALESCE(SUM(CASE WHEN o.status = 'paid' THEN o.total_amount END), 0) AS paid_amount FROM users u LEFT JOIN orders o ON u.id = o.user_id GROUP BY u.id, u.name ORDER BY paid_amount DESC, u.id;",
    explanation: '综合统计题经常要求既保留全量用户，又只统计某种订单状态。',
    interviewTips: ['追问：CASE WHEN 放在 SUM 内部和把条件写在 WHERE 有什么区别？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 12,
  }),
  problem({
    id: 'subquery-where',
    title: '查询最高工资员工',
    chapter: '04 子查询',
    level: 4,
    difficulty: 'Medium',
    frequency: 5,
    description: '查询工资等于全公司最高工资的员工。',
    requirements: ['使用 WHERE 子查询', '返回 id、name、salary'],
    knowledgePoints: ['子查询', '标量子查询', 'MAX'],
    relatedTables: ['employees'],
    starterSql: '-- 查询工资最高的员工\nSELECT id, name, salary\nFROM employees\n',
    solutionSql:
      'SELECT id, name, salary FROM employees WHERE salary = (SELECT MAX(salary) FROM employees) ORDER BY id;',
    explanation: '这是最经典的标量子查询题目之一。',
    interviewTips: ['追问：如果有多名员工并列最高工资，SQL 还能正确返回吗？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 8,
  }),
  problem({
    id: 'subquery-in',
    title: '查询高价值付费用户',
    chapter: '04 子查询',
    level: 4,
    difficulty: 'Medium',
    frequency: 4,
    description: '查询有过 total_amount >= 1000 且状态为 paid 的订单的用户。',
    requirements: ['使用 IN 子查询', '返回 id、name'],
    knowledgePoints: ['IN 子查询', '过滤'],
    relatedTables: ['users', 'orders'],
    starterSql: '-- 查询高价值付费用户\nSELECT id, name\nFROM users\n',
    solutionSql:
      "SELECT id, name FROM users WHERE id IN (SELECT user_id FROM orders WHERE status = 'paid' AND total_amount >= 1000) ORDER BY id;",
    explanation: 'IN 子查询是很多业务筛选题的常见写法。',
    interviewTips: ['追问：IN 和 EXISTS 在大表下各自的适用场景是什么？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 10,
  }),
  problem({
    id: 'exists-subquery',
    title: '查询存在已支付订单的用户',
    chapter: '04 子查询',
    level: 4,
    difficulty: 'Medium',
    frequency: 5,
    description: '查询至少存在一笔已支付订单的用户。',
    requirements: ['使用 EXISTS', '返回 id、name'],
    knowledgePoints: ['EXISTS', '相关子查询'],
    relatedTables: ['users', 'orders'],
    starterSql: '-- 查询存在已支付订单的用户\nSELECT u.id, u.name\nFROM users u\n',
    solutionSql:
      "SELECT u.id, u.name FROM users u WHERE EXISTS (SELECT 1 FROM orders o WHERE o.user_id = u.id AND o.status = 'paid') ORDER BY u.id;",
    explanation: 'EXISTS 更偏向“是否存在匹配记录”的判断。',
    interviewTips: ['追问：EXISTS 子查询里写 SELECT 1 和 SELECT * 有什么区别？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 10,
  }),
  problem({
    id: 'not-exists-subquery',
    title: '查询没有下过订单的用户',
    chapter: '06 综合业务题',
    level: 4,
    difficulty: 'Medium',
    frequency: 5,
    description: '查询从未下过订单的用户。',
    requirements: ['使用 NOT EXISTS', '返回 id、name'],
    knowledgePoints: ['NOT EXISTS', '反向筛选'],
    relatedTables: ['users', 'orders'],
    starterSql: '-- 查询没有下过订单的用户\nSELECT u.id, u.name\nFROM users u\n',
    solutionSql:
      'SELECT u.id, u.name FROM users u WHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.user_id = u.id) ORDER BY u.id;',
    explanation: '“没有做过某件事”的题目一般都能往 NOT EXISTS 想。',
    interviewTips: ['追问：NOT IN 和 NOT EXISTS 在有 NULL 时会有什么差异？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 10,
  }),
  problem({
    id: 'derived-table',
    title: '查询每个用户的最近一次订单',
    chapter: '06 综合业务题',
    level: 4,
    difficulty: 'Medium',
    frequency: 5,
    description: '查询每个下过单用户最近一次订单的时间和金额。',
    requirements: ['使用派生表或子查询', '返回 user_id、name、last_order_at、last_order_amount'],
    knowledgePoints: ['派生表', 'MAX', 'JOIN'],
    relatedTables: ['users', 'orders'],
    starterSql: '-- 查询每个用户最近一次订单\nSELECT \n',
    solutionSql:
      'SELECT u.id AS user_id, u.name, latest.last_order_at, o.total_amount AS last_order_amount FROM users u INNER JOIN (SELECT user_id, MAX(created_at) AS last_order_at FROM orders GROUP BY user_id) latest ON u.id = latest.user_id INNER JOIN orders o ON o.user_id = latest.user_id AND o.created_at = latest.last_order_at ORDER BY u.id;',
    explanation: '这是典型的“先分组求最大，再回表拿完整行”的题型。',
    interviewTips: ['追问：如果同一用户在同一时间有两笔订单，会发生什么？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 12,
  }),
  problem({
    id: 'top-sale-city',
    title: '查询各城市的用户数量排名',
    chapter: '06 综合业务题',
    level: 5,
    difficulty: 'Medium',
    frequency: 4,
    description: '统计每个城市的用户数量，并按用户数量从高到低排序。',
    requirements: ['使用 GROUP BY', '返回 city、user_count'],
    knowledgePoints: ['GROUP BY', '统计排序'],
    relatedTables: ['users'],
    starterSql: '-- 统计各城市用户数量\nSELECT city, \nFROM users\n',
    solutionSql:
      'SELECT city, COUNT(*) AS user_count FROM users GROUP BY city ORDER BY user_count DESC, city;',
    explanation: '这是后续城市 Top N、区域运营分析的基础题。',
    interviewTips: ['追问：如果需要每个城市内部再做分层统计，SQL 怎么扩展？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 8,
  }),
  problem({
    id: 'second-highest-salary',
    title: '查询第二高工资',
    chapter: '06 综合业务题',
    level: 5,
    difficulty: 'Medium',
    frequency: 5,
    description: '查询 employees 表中第二高的工资值。',
    requirements: ['只返回一行一列 second_highest_salary', '需要排除重复工资'],
    knowledgePoints: ['DISTINCT', 'ORDER BY', 'LIMIT'],
    relatedTables: ['employees'],
    starterSql: '-- 查询第二高工资\nSELECT \n',
    solutionSql:
      'SELECT DISTINCT salary AS second_highest_salary FROM employees ORDER BY salary DESC LIMIT 1 OFFSET 1;',
    explanation: '这是最经典的薪资类面试题之一。',
    interviewTips: ['追问：如果没有第二高工资，应该返回什么？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 10,
  }),
  problem({
    id: 'department-top-salary',
    title: '查询每个部门工资最高的员工',
    chapter: '06 综合业务题',
    level: 5,
    difficulty: 'Hard',
    frequency: 5,
    description: '查询每个部门中工资最高的员工，返回部门名、员工名和工资。',
    requirements: ['使用子查询或 JOIN', '允许同部门出现并列最高薪员工'],
    knowledgePoints: ['分组最大值', 'JOIN', '子查询'],
    relatedTables: ['departments', 'employees'],
    starterSql: '-- 查询每个部门工资最高的员工\nSELECT \n',
    solutionSql:
      'SELECT d.name AS department_name, e.name AS employee_name, e.salary FROM employees e INNER JOIN departments d ON e.department_id = d.id INNER JOIN (SELECT department_id, MAX(salary) AS max_salary FROM employees GROUP BY department_id) t ON e.department_id = t.department_id AND e.salary = t.max_salary ORDER BY d.id, e.id;',
    explanation: '这类“每组 Top 1”题是窗口函数出现之前非常常见的写法。',
    interviewTips: ['追问：如果用窗口函数可以怎么写？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 15,
  }),
  problem({
    id: 'monthly-sales',
    title: '按月统计已支付销售额',
    chapter: '06 综合业务题',
    level: 5,
    difficulty: 'Medium',
    frequency: 5,
    description: '按月份统计所有已支付订单的销售额。',
    requirements: ['结果列为 sales_month、sales_amount', '按月份升序排序'],
    knowledgePoints: ['日期分组', 'SUM', '业务统计'],
    relatedTables: ['orders'],
    starterSql: '-- 按月统计已支付销售额\nSELECT \nFROM orders\n',
    solutionSql:
      "SELECT DATE_FORMAT(created_at, '%Y-%m') AS sales_month, SUM(total_amount) AS sales_amount FROM orders WHERE status = 'paid' GROUP BY DATE_FORMAT(created_at, '%Y-%m') ORDER BY sales_month;",
    explanation: '业务报表非常常见的月度统计题。',
    interviewTips: ['追问：如何继续按城市或品类维度细分销售额？'],
    expectedResult: { comparison: 'rows', ordered: true },
    allowedOperations: ['SELECT'],
    estimatedMinutes: 10,
  }),
]

export const mysqlProblemsById = Object.fromEntries(
  mysqlProblems.map((item) => [item.id, item]),
) as Record<string, MysqlProblem>

export function getMysqlProblemById(problemId: string) {
  return mysqlProblemsById[problemId]
}

export const mysqlChapters = Array.from(
  new Set(mysqlProblems.map((problemItem) => problemItem.chapter)),
)
